#lang info

;; pkg info

(define version "0.1")
(define collection "noise")
(define deps '("base"
               ["crypto-lib" #:version "1.5"]
               ["binaryio-lib" #:version "1.1"]))
(define pkg-authors '(ryanc))

;; collection info

(define name "noise")
